class Entity { 
    constructor(firstName, lastName, sex, species) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.sex = sex;
        this.species = species;
    }
    getData()
    {
        return console.log(this.name + " " + this.race);
    }
}